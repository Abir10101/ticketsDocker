name: Testing

on: [push, pull_request]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: trigger jenkins Job
      uses: appleboy/jenkins-action@master
      with:
        url: "http://172.105.36.103:8080"
        user: "abir101"
        token: ${{ vars.JENKINS_TOKEN }}
        job: "ga1"
