pipeline {
    agent any
    stages {
        stage('Check Tag Name') { 
            steps {
                script{
                    if( '${tag_name}' == -1 || '${tag_name}' == "" )
                    {
                        error("Tag Name Empty")
                    }
                }
            }
        }
    }
}
