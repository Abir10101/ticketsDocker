pipeline {
    agent any
    stages {
        stage('Check Tag Name') { 
            steps {
                if( '${tag_name}' == -1 )
                {
                    error("Tag Name Empty")
                }
            }
        }
    }
}
