pipeline {
    agent any
    stages {
        stage('Echoing') { 
            steps {
                sh '''
                echo ${tag_name}
                '''
                error("Build failed because of this and that..")
            }
        }
    }
}
